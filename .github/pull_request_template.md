<!-- Drei Fragen. Nicht mehr, und das mit Absicht: Eine Vorlage, die man abarbeitet,
     ohne nachzudenken, ist kein Nachdenken, sondern ein Formular. Diese drei sind so
     gewählt, dass man sie nicht beantworten kann, ohne kurz nachgedacht zu haben.
     Die Antworten sind für den nächsten Menschen gedacht, nicht für ein Programm. -->

## Was diese Arbeit tut

<!-- Zwei, drei Sätze in normalem Deutsch. Was ändert sich für den, der Onda benutzt. -->

## 1. Welches Revier

**Welchen der sechs Ordner aus `betrieb/REVIERE.md` fasst diese Arbeit an?**

<!-- betrieb, app, mac, tools, design, docs. Mehr als eines ist erlaubt und kein Fehler.
     Es ist nur der eine Fall, bei dem andere davon wissen müssen. -->

Antwort:

## 2. Welcher Leitstand-Eintrag

**Welche Datei unter `betrieb/leitstand/` beschreibt diesen Zweig?**

<!-- Der Dateiname ist der Zweigname mit Bindestrichen statt Schrägstrichen. Die Regel
     dahinter steht in betrieb/LEITSTAND.md; `node betrieb/waechter/leitstand.mjs` sagt,
     ob die Datei da ist. Er prüft ihre Anwesenheit, nicht ihren Inhalt — ob der Eintrag
     wirklich sagt, was hier passiert, sieht nur ein Mensch. -->

Antwort:

## 3. Welcher Wächter

**Welche Prüfung zeigt, dass das hier stimmt, und was hat sie gemeldet?**

<!-- Ein Befehl, den man tippen kann, und seine echte Ausgabe. Zum Beispiel
     `cd app && npm run test:unit` oder `node betrieb/waechter/alle.mjs`.
     Wenn es für diese Änderung keine Prüfung gibt, ist das die ehrliche Antwort —
     dann steht hier, woran ein Mensch es stattdessen nachsehen kann. -->

Antwort:
